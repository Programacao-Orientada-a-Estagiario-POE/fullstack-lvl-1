{
  "name": "oop",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig-build.json",
    "start": "node dist/index.js",
    "dev": "tsnd -r tsconfig-paths/register --no-notify --inspect=127.0.0.1:9230 --transpile-only --ignore-watch node_modules --respawn src/index.ts"
  },
  "devDependencies": {
    "@types/signale": "^1.4.2",
    "ts-node-dev": "^1.1.6",
    "tsconfig-paths": "^3.10.1",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "signale": "^1.4.0"
  }
}
